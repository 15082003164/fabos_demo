<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yangy.springboot.dao.UserRoleDao">
    <!-- 用户角色绑定-->
    <insert id="bondUserAndRole" parameterType="com">
        insert into user_role(id,role_id) values(#{userId},#{roleId});
    </insert>
</mapper>